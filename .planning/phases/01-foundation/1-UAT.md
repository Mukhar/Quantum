---
status: testing
phase: 01-foundation
source: [.planning/phases/01-foundation/PLAN.md]
started: 2026-06-29T16:18:00Z
updated: 2026-06-29T16:21:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 2
name: CircuitView "Copy as Qiskit" button on /cnot-bell (QSK-02)
expected: |
  Open http://localhost:4321/cnot-bell. Below the circuit SVG you
  should see a small green "Copy as Qiskit" link/button on the right
  (alongside the existing "Remix in sandbox →" link). Click it — the
  button text flashes "Copied!" for ~1.5s, then returns to
  "Copy as Qiskit". Paste from clipboard: starts with
  "# Generated by quantum-site v3 — circuit fragment ..." followed by
  the caption, then `from qiskit import QuantumCircuit` and
  `qc = QuantumCircuit(2, 2)` + `qc.h(0)` + `qc.cx(0, 1)`.
awaiting: user response

## Tests

### 1. Sandbox "Copy as Qiskit" toolbar button (QSK-01)
expected: |
  /sandbox toolbar shows green "Copy as Qiskit" button between
  Copy share URL and Save. Click on a built circuit copies a runnable
  Qiskit snippet to the clipboard and surfaces a toast.
result: pass
notes: First attempt with empty default circuit correctly emitted bare QuantumCircuit(2,2) stub. After adding H(0) + CNOT(0,1), paste contained qc.h(0) and qc.cx(0, 1). Wiring + emission verified.

### 2. CircuitView "Copy as Qiskit" button on /cnot-bell (QSK-02)
expected: |
  Open http://localhost:4321/cnot-bell. Below the circuit SVG you
  should see a small green "Copy as Qiskit" link/button on the right
  (alongside the existing "Remix in sandbox →" link). Click it — the
  button text flashes "Copied!" for ~1.5s, then returns to
  "Copy as Qiskit". Paste from clipboard: starts with
  "# Generated by quantum-site v3 — circuit fragment ..." followed by
  the caption, then `from qiskit import QuantumCircuit` and
  `qc = QuantumCircuit(2, 2)` + `qc.h(0)` + `qc.cx(0, 1)`.
result: [pending]

### 3. CircuitView "Copy as Qiskit" button on /deutsch (QSK-02)
expected: |
  Open http://localhost:4321/deutsch. Each `CircuitView` on the page
  has its own "Copy as Qiskit" button. Click any one — flashes
  "Copied!" → clipboard has `qc = QuantumCircuit(1, 1)` (or 2 depending
  on the variant) and at least one `qc.rx(...)` with the angle in full
  precision (e.g. `3.1415926535897931`).
result: [pending]

### 4. Bundle gate fail-mode is readable when exceeded (OPS-04 ergonomics)
expected: |
  I will temporarily lower one route's ceiling in bundle-budget.json,
  rerun `npm run check:bundle`, capture the output, and revert. You
  confirm the failure output names the offending route + actual vs
  ceiling and suggests next steps.
result: [pending]

### 5. (Optional) Pasted snippet actually runs in a Python shell with Qiskit
expected: |
  Cross-plan acceptance: paste the Bell-pair snippet from test 1 (or
  2) into a Python shell with Qiskit installed and run it. The
  snippet must execute without `SyntaxError` and produce a valid
  `QuantumCircuit`. Skip if Qiskit isn't installed locally — the unit
  test suite covers correctness for every gate via the QSK-03 drift
  gate.
result: [pending]

## Summary

total: 5
passed: 1
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps

[none yet]
