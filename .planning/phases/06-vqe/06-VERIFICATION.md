---
status: passed
phase: 06-vqe
verifier: gsd-executor (06-08 close-out)
verified_at: 2026-06-30T21:15:00Z
basis:
  - Automated: npm test green (658 / 658 tests across 44 vitest files)
  - Automated: npm run build green (24 pages emitted in 2.17s)
  - Automated: npm run check:bundle green (all 24 routes within post-recompute budget)
  - Source verification: `git diff 4dafc34..HEAD -- src/lib/quantum/simulator.ts src/lib/quantum/circuit.ts package.json package-lock.json` → empty (0 lines changed across 4 files spanning entire Phase 6)
  - Source verification: `tests/essays/nav-graph.test.ts` CHAIN[] = 12 essays (qubit → … → vqe)
  - Source verification: `tests/essays/concept-map.test.ts` expectedEssays[] = 12 essays (same chain)
  - Code-level a11y review: structural a11y verified across all v3 widgets (aria-live politeness, keyboard reach, focus rings, descriptive external link text, mobile horizontal-scroll affordances, marker drag with keyboard equivalent, reduced-motion handling, visited-state announcement on concept map)
  - Manual user gate deferred: formal Lighthouse mobile a11y ≥ 95 run + live screen-reader pass on /vqe widgets remain user-side (per OPS-01 + LIGHTHOUSE-PLAN.md)
---

# Phase 6 — VQE + Chemistry + v3 launch — VERIFICATION

## Goal recap

> Ship the final v3 phase. Land `/vqe` (variational/hybrid algorithm essay:
> vanilla-TS optimizer + `EnergyLandscape` + `MoleculeGallery`), the
> local-only PROG-01 concept-map visited indicator across all v3 essays,
> and the v3 launch close-out (LIGHTHOUSE-PLAN, BUNDLE-AUDIT including
> the concept-map layout audit, V3-LAUNCH-ANNOUNCEMENT). After this phase,
> v3.0 — Algorithms × Use Cases is feature-complete.

## Success criteria scorecard (vs PLAN.md Definition of Done, 12 items)

| # | Criterion | Status | Evidence |
|---|---|--------|----------|
| 1 | **VQE optimizer module exists.** `src/lib/quantum/vqe.ts` exports finite-difference GD-with-momentum optimizer + analytic H₂ surface + sampler/interpolator + reseed seeds. Tests cover convergence on 1D parabola and on `h2Energy` within `1e-3` of true minimum. Helpers re-exported via `src/lib/quantum/index.ts`. | ✅ PASS | ~330 LOC module (commit `4848dfb`). Exports: `gradientDescent`, `h2Energy`, `H2_TRUE_MINIMUM = {θ₁:π, θ₂:π, energy:−1.137}`, `sampleSurface`, `interpolateSurface`, `RESEED_SEEDS` (6 deterministic pairs), `DEFAULT_INITIAL_THETAS`. Central FD gradient + Polyak momentum + one-shot adaptive guard (D-02 explicit: at most one retry per iteration). Peruzzo et al. *Nature Comms* 5, 4213 (2014) citation embedded for the −1.137 Hₐ reference (D-07). `tests/quantum/vqe.test.ts` (23 tests): 1D parabola converges within `1e-4` in 13 iterations; `h2Energy` converges within `1e-3` Hₐ from every one of the 6 `RESEED_SEEDS` starts; determinism gate passes. Barrel re-exports in `src/lib/quantum/index.ts`. |
| 2 | **Molecule data exists.** `src/data/molecules.json` ships H₂, LiH, HeH⁺ with all required fields (Hartree+eV under documented conversion, ansatz `Op[][]` valid under `validateCircuit`, qubits ≤ 4, `precomputed_note`). Schema/data tests assert all of above plus Qiskit-export validity. | ✅ PASS | `src/data/molecules.json` (new, first inhabitant of `src/data/`) keys 3 molecules (`h2`, `hehplus`, `lih`) each with id/name/formula/`equilibrium_distance_angstrom`/qubits/`ansatz_params`/`ansatz_ops`/`energy_hartree`/`energy_ev`/`precomputed_note`/`source` (commit `3a507df`). `src/data/molecules.ts` typed shim re-exports `MOLECULES`, `MOLECULE_IDS`, `HARTREE_TO_EV = 27.211386245988`, `Molecule` interface. `tests/data/molecules.test.ts` (30+ tests across 7 suites) asserts schema, Hartree↔eV consistency within numerical tolerance under the documented conversion, ansatz round-trip through `validateCircuit()`, all `qubits ≤ MAX_QUBITS = 4`, `precomputed_note` present, and parameterised `toQiskit(circuit)` numeric round-trip within `1e-4` for all three molecules (robust against IEEE-754 representation of decimal JSON literals). |
| 3 | **EnergyLandscape widget exists.** SSR 50×50 heatmap + contour bands using brand-aligned 2-color gradient (semantic CSS vars, no new dep). Draggable marker (pointer + keyboard), bilinear interpolation, Auto-descend animating along `vqe.ts` trajectory, Reseed cycling deterministic seed set. `prefers-reduced-motion` collapses animation to final-state snap. Accessible text readouts beside visual. | ✅ PASS | `src/components/EnergyLandscape.astro` (~460 LOC, commit `0bfa8a1`). SSR `sampleSurface(h2Energy, 50, [0, 2π])` at build time → 2 500 `<rect>` cells colored by 2-stop indigo-500 → amber-500 brand gradient. Isoband contour overlay built from per-cell bucket-boundary `<line>` segments (plan-allowed simpler alternative to marching-squares). `(π, π)` true-minimum cross-hair, axis ticks at `{0, π/2, π, 3π/2, 2π}`. Marker `<circle role="slider" tabindex="0" aria-orientation="other">` with focusable halo. Hydrator imports `gradientDescent` / `h2Energy` / `interpolateSurface` from `../lib/quantum/vqe` and `signal<T>` from `../lib/sandbox/signal`. Wires: pointer drag with `setPointerCapture`, keyboard ArrowKeys (`2π/100` nudge), Home (jump to `H2_TRUE_MINIMUM`), Auto-descend (60 ms/frame; `aria-busy="true"` during animation; `matchMedia("(prefers-reduced-motion: reduce)").matches` path snaps to converged frame — D-16), Reseed (modulo cycle through `RESEED_SEEDS`). Accessible θ₁ / θ₂ / energy readouts beside the visual. `tests/components/energy-landscape.test.ts` (12 structural assertions). |
| 4 | **MoleculeGallery widget exists.** Consumes `molecules.json` at SSR, one card per molecule with name+formula, equilibrium slider (frozen, labelled), ansatz readout, energy in both Hartree and eV, Copy-as-Qiskit via `toQiskit()`. No live ab initio recomputation. | ✅ PASS | `src/components/MoleculeGallery.astro` (~153 LOC, commit `0bfa8a1` source / `2232b0e` summary). SSR card grid of 3 molecules. Each card: name + formula heading; `disabled` range slider labelled `"equilibrium · slider is illustrative"` (D-25 honesty rule — no live recomputation); monospace `θ₁ = …, θ₂ = …` readout; converged energy in both Hartree and eV (D-26): e.g. `-1.1370 Hₐ (-30.94 eV)`; embedded `<CircuitView circuit={…} showRemixLink={false}>` reuses existing baked-at-SSR `data-qiskit` attr + page-wide event-delegated `copy-qiskit` handler (per-card Copy-as-Qiskit at zero JS cost — D-18); visible `precomputed_note` + text source citation. `tests/components/molecule-gallery.test.ts` (11 assertions across 2 suites): structural source-grep (`data-widget`, `data-molecule-id`, no `Math.random` / `crypto.getRandomValues` / `Date.now` / `performance.now`, `validateCircuit` called at frontmatter time) + parameterised per-molecule `validateCircuit` + `toQiskit` round-trip within `1e-4`. |
| 5 | **`/vqe` essay exists.** Two-half essay via `EssayLayout`. Algorithm half: hook → static `CircuitView` ansatz + slim live readout (D-18/D-19) → `EnergyLandscape` → hand-off. Use-case half: chemistry framing (sourced/caveated per D-27..D-30) → `MoleculeGallery` → reality check → Qiskit CTA → self-test → footer-nav (prev /shor, next /sandbox). MathNerds appendix covers gradient + update rule. | ✅ PASS | `src/pages/vqe.astro` (commit `24ba1f0`). Two-half rhythm matching `superdense-coding.astro` / `shor.astro` / `teleportation.astro`. Algorithm half: hook ("the quantum computer tries a wavefunction; the classical computer turns the knobs") → static `CircuitView` of `Ry(θ₁) ⊗ Ry(θ₂) → CNOT(0,1)` at `DEFAULT_INITIAL_THETAS` (D-18) → slim `<p data-role="ansatz-readout" aria-live="polite">` above landscape (D-19) → `EnergyLandscape` → hand-off (D-29). Use-case half: near-term industrial framing with sourced primary-source DOI citations (Peruzzo 2014, Kandala 2017, Cao 2019) → `MoleculeGallery` → explicit reality check ("classical baselines like HF and CCSD remain hard competition"; noise + barren-plateau training pathologies) → Qiskit CTA. Strictly no-hype prose (D-30, verified by `tests/essays/vqe.test.ts > sourced no-hype framing tokens present, D-30 hype tokens absent`). Self-test 2 questions + MathNerds appendix (Rayleigh-Ritz energy functional, central FD gradient with parameter-shift rule footnote, GD-with-momentum update rule mirroring `vqe.ts`). Footer-nav `prev /shor`, `next /sandbox`. Inline `<script>` wires `vqe:thetachange` event listener that mirrors EnergyLandscape's marker into the slim ansatz readout. |
| 6 | **All same-commit mirrors updated.** ConceptMap node + edge, concept-map test mirrors (`expected[]` + `expectedEssays[]`), nav-graph `CHAIN[]` (insert `/vqe` + rewire `shor.next`), sandbox-links `STARTERS` adds VQE ansatz, `bundle-budget.json` `/vqe` entry, `sitemap.xml.ts` `/vqe` at priority 0.9, `shor.astro` footer-nav rewires `/sandbox → /vqe`. | ✅ PASS | All 7 mirrors landed in commit `24ba1f0` alongside the essay: (1) `ConceptMap.astro` — new `/vqe` primary node at `(cx=670, cy=430)` on row 4, new reading-path edge `12 → 13`, `aria-label` updated to "12 essays in reading order (5 foundations + 7 algorithm)"; (2) `shor.astro` — footer-nav next rewired `/sandbox → /vqe` + header comment updated; (3) `sitemap.xml.ts` — `/vqe` at priority `0.9`, slotted after `/shor`; (4) `bundle-budget.json` — `"vqe": 4096` placeholder (later recomputed to 3072 in 06-08); (5) `tests/essays/concept-map.test.ts` — both `expected[]` and `expectedEssays[]` include `/vqe`; (6) `tests/essays/nav-graph.test.ts` — `CHAIN[]` rewires `shor.next` to `/vqe` and appends `{slug:"vqe", prev:"/shor", next:"/sandbox"}`; (7) `tests/essays/sandbox-links.test.ts` — `STARTERS["vqe"] = Ry|Ry|CNOT(0,1)` at `DEFAULT_INITIAL_THETAS`. Mirror discipline (D-45) preserved; all tests green. |
| 7 | **PROG-01 visited indicator works.** `src/lib/progress.ts` reads/writes `localStorage["quantum/visited"]` JSON array, dedupes, tolerates storage failures (D-31..D-35). Scroll-depth observer in `EssayLayout` flips flag at 50% (D-31). Concept-map hydrator brightens visited nodes in both desktop SVG and mobile list, paired with `aria-label` announcement (D-34). No analytics / beacon / server (D-33). | ✅ PASS | `src/lib/progress.ts` (new, commit `63c771c`) exports `VISITED_KEY = "quantum/visited"` (D-32), `VISITED_THRESHOLD = 0.5` (D-31), `getVisited()` / `markVisited(path)` / `clearVisited()` / `instrumentScrollDepth(currentPath)`. Every `localStorage` access wrapped in `try/catch` matching `src/lib/theme.ts` + `src/lib/annotations.ts` (D-35). `getVisited` returns `[]` on missing key / throwing `getItem` / non-JSON / wrong-shape / non-string items, dedupes on read; `markVisited` dedupes on write and is silent on storage failure; `instrumentScrollDepth` uses passive `scroll` listener, self-removes on first trigger per path, idempotent per-page-session via module-level `triggered` set, treats short pages (`scrollHeight - innerHeight ≤ 0`) as fully-read on first scroll. `src/layouts/EssayLayout.astro` — single inline `<script>` import + call instruments every essay using EssayLayout (D-36). `src/components/ConceptMap.astro` — hydrator imports `getVisited`, marks `data-visited="true"` on matching `<a>` in BOTH desktop SVG and mobile `<ol>` fallback via one `querySelectorAll('a[href]')` (or explicit `svg a` + `ol a`), updates `aria-label` to `"Read: <Label> (visited)"` (D-34). Style block lifts brightness via `--color-accent-muted` (fill) + `--color-accent-emphasis` (stroke + text); both themes; no tick badge / icon (D-34). 19 new tests (`tests/progress/progress.test.ts` 11 + `tests/progress/instrument-scroll.test.ts` 3 + `tests/components/concept-map-progress.test.ts` 5). |
| 8 | **OPS audits authored.** `LIGHTHOUSE-PLAN.md` (manual-run recipe covering every v3 route in both themes; route count from final post-`/vqe` build), `BUNDLE-AUDIT.md` (final bundle delta vs v2 + per-route deltas/ceilings + concept-map layout audit subsection per OPS-03). | ✅ PASS | Both files at user-locked paths (commit `93c4640`). `LIGHTHOUSE-PLAN.md`: manual-run recipe targeting all 24 v3 routes in both `prefers-color-scheme: light` and dark; mobile a11y ≥ 95 hard target; v1+v2 baselines table; per-route expected-score table with separate "v3 essay routes (new)" subsection; pre-known a11y concerns catalogued (`EnergyLandscape` marker drag, `MoleculeGallery` focus order, PROG-01 visited announcement, `LargeCircuitView` scroll region carrying from 5b, reduced-motion verification); pre-known perf concerns (KaTeX CDN, Sphere3D 130 KB gzipped lazy chunk, EnergyLandscape 2500-rect SSR heatmap); verdict intentionally blank pending launch-day manual run. `BUNDLE-AUDIT.md`: method, top-line v1→v2→v3 numbers (24 pages / 45 JS chunks / 184.9 KB total gzipped JS site-wide / 32 KB CSS / Sphere3D 129.8 KB gzipped still dominant), top-5 heaviest gzipped chunks, per-page initial JS payload pre-recompute snapshot, v3 line-item additions per phase, bundle isolation spot-checks (every essay route HTML grep'd for `/vqe`-specific tokens → 0 occurrences). Embedded concept-map layout audit subsection (D-37..D-39, OPS-03): full geometry inspection (14-node four-row layout on 1000×460 SVG viewBox with 40 px clear gaps in longest rows and 64–74 px between rows); **FLAT verdict**. |
| 9 | **V3 launch announcement drafted.** `V3-LAUNCH-ANNOUNCEMENT.md` matching v2 tone/structure scoped to v3 narrative: 5 algorithm × use-case essays + Qiskit bridge + local-only progress indicator + no-analytics / no-backend re-ratification (D-30, D-33, D-40, OPS-02). | ✅ PASS | `.planning/phases/06-vqe/V3-LAUNCH-ANNOUNCEMENT.md` (new, ~360 lines, commit `a9dc659`). Sections: lede ("where it actually bites reality" per D-27); numbered `(1)` five algorithm × use-case essays with per-essay honest caveats (Grover √N, superdense ≠ free bandwidth, RSA not on a date, VQE not drug discovery solved — D-30); `(2)` Qiskit bridge ships everywhere (sandbox toolbar + every essay `CircuitView` + every molecule card + static N=15 Shor); `(3)` PROG-01 local-only progress indicator re-ratifying no analytics / no server / no accounts (D-33); "What v3 deliberately is NOT" reasserts no analytics ever + no backend + no live ab initio chemistry + no live N=15 factoring + no 8+-qubit simulator + no best-PQC recommendation + no RSA calendar date + still no Playwright visual regression; Try-it-now route bullet list; Under-the-hood final stats (24 pages, +10 KB gzipped eager JS site-wide); 658 test count; HN title + tweet + r/QuantumComputing + r/programming variant blurbs; 9-step launch-day checklist (deploy → smoke-test routes → smoke-test Qiskit bridge → smoke-test progress indicator → clear visited storage → formal Lighthouse → confirm budget → spot-check dark theme → announce); hotfix protocol. `<URL>` / `<REPO>` placeholders match v1/v2 convention. |
| 10 | **Bundle ceilings recomputed.** Plan 06-08 runs clean build and recomputes every changed route's ceiling using `round_up(actual_gzip × 1.2, 1024)`. `bundle-budget.json` updated in same commit as recompute. | ✅ PASS | `bundle-budget.json` recompute in commit `a9dc659` (same commit as `V3-LAUNCH-ANNOUNCEMENT.md`). Two routes touched: `/vqe` 4096 → 3072 B (tightens; placeholder 4 KB was conservative anticipation of inline surface data, but the 50×50 heatmap landed mostly in SSR HTML — eager JS at 2.3 KB, headroom 1.7 KB > 1 KB recompute threshold per user-locked rule); `/deutsch` 1024 → 2048 B (PROG-01 scroll observer pushed actual from 0.7 KB → 0.9 KB, one byte from gate; next 1024 boundary restores normal 20 % cushion). All 22 other routes already snap to same 1024 boundary the recompute would produce; no diff. Same-commit OPS-04 discipline (D-43) preserved. |
| 11 | **No new runtime dependencies.** `package.json` and `package-lock.json` remain untouched across all 8 plans. Vanilla TS + Astro + existing `signal<T>` only. | ✅ PASS | `git diff 4dafc34..HEAD -- package.json package-lock.json` → empty (0 lines changed across both files spanning entire Phase 6, from Phase 5b close-out at `4dafc34` to Phase 6 close-out at `HEAD`). No charting library, no graph library, no 3D library, no analytics SDK, no backend. VQE optimizer is ~330 LOC vanilla TS; EnergyLandscape is SSR + `signal<T>` reactive primitive; PROG-01 helper is ~80 LOC vanilla TS. |
| 12 | **Build, tests, and bundle gate all green** at phase close: `npm test` passes (548 baseline + new suites); `npm run build` emits new `/vqe` page cleanly (23 → 24 routes); `npm run check:bundle` passes for every route including `/vqe`. | ✅ PASS | At commit `a9dc659`: `npm test` → **658 / 658** passing across 44 vitest files in 5.27s (+110 from Phase 5b baseline of 548: 23 vqe + 30 molecules + 12 energy-landscape + 11 molecule-gallery + 8 vqe-essay + 11 progress unit + 3 progress integration + 5 concept-map-progress + 7 redistribution across mirror edits). `npm run build` → **24** page(s) built in 2.17s; no warnings; `dist/vqe/index.html` emits cleanly. `npm run check:bundle` → all 24 routes within new budget; `/vqe` at 2.3 / 3.0 KB; `/deutsch` at 0.9 / 2.0 KB. |

## Plans landed (atomic commits, 8 plans across 4 waves)

| Plan  | Source commit | Summary commit | Title |
|-------|---------------|----------------|-------|
| 06-01 | `4848dfb` | `7a252c1` | `feat(06-vqe/06-01): vqe.ts optimizer + h2Energy + tests` |
| 06-02 | `3a507df` | `b64ff27` | `feat(06-vqe/06-02): molecules.json (H2, LiH, HeH+) + schema tests` |
| 06-03 | `0bfa8a1` | `cc112d4` | `feat(06-vqe/06-03): EnergyLandscape SSR heatmap + drag/keyboard/Auto-descend` |
| 06-04 | `0bfa8a1`* | `2232b0e` | `docs(06-vqe/06-04): SUMMARY.md — MoleculeGallery + Qiskit export` (*source swept into 06-03 commit due to parallel-wave race; no functional defect — documented in 06-03-SUMMARY.md) |
| 06-05 | `24ba1f0` | `c17b052` | `feat(06-vqe/06-05): /vqe essay + mirrors (concept-map, nav, sandbox-links, budget, sitemap, shor footer)` |
| 06-06 | `63c771c` | `5e32d0d` | `feat(06-vqe/06-06): PROG-01 visited indicator (progress.ts + scroll observer + concept-map hydrator)` |
| 06-07 | `93c4640` | `09ba2af` | `docs(06-vqe/06-07): LIGHTHOUSE-PLAN.md + BUNDLE-AUDIT.md + concept-map layout audit` |
| 06-08 | `a9dc659` | *(this close-out)* | `docs(06-vqe/06-08): V3-LAUNCH-ANNOUNCEMENT.md + final verification + ceiling recompute` |

## Numbers (post-phase)

- **Tests:** **658 / 658** passing across 44 vitest files (+110 from Phase 5b baseline of 548).
- **Pages:** **24** building clean in 2.17s (+1 from Phase 5b: `/vqe`).
- **Bundle (post-recompute):**
  - `/vqe`: 2.3 / 3.0 KB.
  - `/deutsch`: 0.9 / 2.0 KB.
  - Site-wide: ~184.9 KB total gzipped JS across 45 chunks; v3's eager-JS additions total ~10 KB site-wide across 5 new essays.
- **Footer-nav chain (final):** `qubit → superposition → measurement → gates → entanglement → cnot-bell → deutsch → teleportation → superdense-coding → grover → shor → vqe → sandbox`.
- **Phase-6 source diffs from baseline `4dafc34`:**
  - `src/lib/quantum/simulator.ts`: 0 lines changed.
  - `src/lib/quantum/circuit.ts`: 0 lines changed.
  - `package.json`: 0 lines changed.
  - `package-lock.json`: 0 lines changed.

## Notable correctness observations

- **VQE optimizer determinism is locked.** `tests/quantum/vqe.test.ts > determinism > two runs with identical opts produce toEqual-identical OptimizerStep[] arrays`. No `Math.random`, no `Date.now()`. Auto-descend animation plays the same trajectory the unit tests pin.
- **H₂ surface citation is mandatory.** Top-of-file Peruzzo et al. *Nature Comms* 5, 4213 (2014) reference establishes the −1.137 Hₐ FCI baseline for the analytic 2-parameter surface (D-07). Future edits to `h2Energy` that drift the minimum would fail the convergence-per-seed tests.
- **Molecule numeric round-trip robust against IEEE-754.** `tests/data/molecules.test.ts` and `tests/components/molecule-gallery.test.ts` parameterise over all three molecules and assert `toQiskit(circuit)` contains every rot op's converged θ within `1e-4` (robust against `-0.142 → -0.14199999999999999` under `toPrecision(17)`).
- **PROG-01 storage-failure tolerance covers 5 distinct failure modes.** `getVisited` returns `[]` for: (1) missing key, (2) `localStorage.getItem` throws, (3) non-JSON garbage, (4) wrong-shape JSON (not an array), (5) array containing non-string items. Each mode has its own `it()` in `tests/progress/progress.test.ts`.
- **Concept-map visited hydrator works in both surfaces.** `tests/components/concept-map-progress.test.ts` asserts the hydrator catches both SVG `<a>` and mobile `<ol> <a>` via a single `a[href]` query (or explicit `svg a` + `ol a`). Brightness lift uses semantic accent tokens; works in both themes.
- **Concept-map FLAT layout reads cleanly.** 06-07 audit: 14-node four-row layout on 1000×460 SVG viewBox with 40 px clear gaps in longest rows and 64–74 px between rows. No accidental edge crossings, logical visual reading order. Track grouping (D-39 contingency) not invoked.
- **Bundle isolation preserved across v3.** Every essay route HTML grep'd for `/vqe`-specific tokens (`EnergyLandscape` / `h2Energy` / `MoleculeGallery`) → 0 occurrences. The `EnergyLandscape` 50×50 surface JSON is `/vqe`-only as designed.
- **`MAX_QUBITS = 4` hard cap survived 6 v3 algorithm phases.** Teleportation (2-3 qubits), Superdense (2 qubits), Grover (4 qubits max in essay), Shor 5a (4-qubit QFT), Shor 5b (8-qubit N=15 lives in separate `StaticCircuitOp` module per 5b D-16), VQE (2-qubit ansatz). Zero `simulator.ts` / `circuit.ts` changes in any phase.

## Gaps

None blocking. Three user-side gates remain open and have been deferred per OPS-01 + LIGHTHOUSE-PLAN.md (matching the 5b deferral pattern):

- **Formal Lighthouse mobile a11y ≥ 95** (light + dark themes) on the deployed v3 routes — structural a11y is in place across every v3 widget (verified by structural tests and component review at each phase close-out); formal device-emulated Lighthouse rolls up at launch day per the recipe in `LIGHTHOUSE-PLAN.md`. **DEFERRED** to launch-day manual gate.
- **Live screen-reader pass** (VoiceOver / NVDA) over the v3 widgets — `EnergyLandscape`, `MoleculeGallery`, `LargeCircuitView` (carry from 5b), `RSACountdown`, and the PROG-01 visited-state announcement. **DEFERRED**.
- **Real-device mobile check** of `LargeCircuitView` horizontal-scroll behaviour (carry from 5b). **DEFERRED**.

## Carry-forward (operational)

- **v3.0 — Algorithms × Use Cases — is feature-complete.** Deploy is a parallel ops task carrying over from v1 + v2 (see `.planning/STATE.md` "Parallel ops tasks"). Phase 6 ships code-complete v3.0; deployment is independent of phase work.
- **All 24 v3 routes within bundle budget; OPS-04 healthy heading into launch.**
- **The 4-qubit cap is now defensible across the entire algorithm canon.** Six v3 algorithm phases, zero `MAX_QUBITS` change. Qiskit-export bridge handles every algorithm whose canonical form exceeds the cap.
- **658 tests / 24 pages / all routes green is the v3.0 launch baseline.**

## Verdict

**Phase 6 — VQE + Chemistry + v3 launch — is COMPLETE.** All 12 phase-level DoD criteria PASS. Three operational items (formal Lighthouse, live screen-reader pass, real-device mobile check) are DEFERRED to launch-day manual gates per OPS-01 + LIGHTHOUSE-PLAN.md; structural a11y is in place.

**v3.0 — Algorithms × Use Cases — is FEATURE-COMPLETE.** Deploy is a parallel ops task.
